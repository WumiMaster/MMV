# 产品需求文档（PRD）

> **文档性质**：项目总览 + 产品需求 + 阶段规划  
> **项目状态**：暂未定名（需后续命名）  
> **文档版本**：v1.0  
> **整理日期**：2026-07-27  
> **目标读者**：零基础项目发起人、后续开发人员、管理员

---

## 一、文档说明

本文档基于项目发起人的全部需求整理而成，是本项目开发过程中**唯一最高优先级依据**。所有功能边界、开发节奏、技术决策规则均以此为准。任何新增功能、技术选型、架构调整，都必须回到本文档进行核对，并由项目发起人最终确认。

---

## 二、最高优先级硬性规则（全程严格遵守）

### 2.1 技术选型必须由项目发起人决定

项目发起人属于**编程零基础**，不具备技术判断能力。因此：

- **禁止擅自选定**：任何技术框架、第三方组件、部署方案、数据库、实时语音方案、云服务器等，均不得由开发侧单方面决定。
- **必须提供多套方案**：每个技术决策点，至少提供 **2 套及以上** 可行方案。
- **每套方案必须包含**：
  - 方案名称与组成
  - 优势（用通俗语言说明）
  - 劣势（用通俗语言说明）
  - 部署难度（入门 / 中等 / 复杂）
  - 长期运行成本（低 / 中 / 高，含服务器、域名、流量等）
  - 通俗解释技术差别
  - 详细部署步骤（阶段1至少）
- **最终选择由项目发起人确认**，未确认前不得进入下一步开发。

> 本规则贯穿整个项目开发周期，任何阶段均不得违反。

### 2.2 分阶段迭代开发，禁止一次性全部实现

必须严格按照以下阶段推进，**每完成一个阶段，经项目发起人确认可用后，方可启动下一阶段**。

| 阶段 | 目标 |
|------|------|
| 阶段1 | 搭建前后端基础框架、登录注册页面、管理员后台基础页面（只实现用户 CRUD）、完成基础 UI 风格搭建 |
| 阶段2 | 实现频道创建、加入频道、文字子频道、图文聊天、消息持久化 |
| 阶段3 | 开发 Web 实时语音子频道、音频控制、说话状态检测 |
| 阶段4 | 完善悬浮多窗口系统、动画、个人设置、全部细节优化 |
| 阶段5 | BUG 修复、兼容性优化，支持移动端浏览器适配 |

### 2.3 功能边界：禁止开发需求以外的功能

**明确不开发**：

- 好友系统
- 私信（DM）功能
- 表情包商店
- 直播功能
- 机器人系统
- 付费 / 会员 / 订阅
- 排行榜 / 等级 / 成就
- 广告系统
- 游戏集成
- 社群发现 / 推荐

**仅开发文档明确列出的功能**。

### 2.4 代码与配置规范

- 代码结构必须分层清晰，目录结构易懂。
- 关键逻辑必须添加**中文注释**。
- 重要配置（数据库地址、密钥、端口号、API 地址等）统一放入环境配置文件。
- **禁止硬编码密钥、地址、密码等敏感信息**。
- 提交代码前需清理调试代码。

### 2.5 部署与兼容性

- 项目必须支持在主流浏览器（Chrome、Edge、Firefox、Safari）中运行。
- 移动端浏览器需可访问（阶段5重点优化）。
- 麦克风音频采集需满足浏览器安全策略：**仅允许在 `https://` 或 `localhost` 环境下调用**。
- 必须提供配套的部署操作说明。

### 2.6 Git 版本管理规则

**所有 Git 操作必须经项目发起人授权后方可执行，禁止擅自操作。**

以下操作需要项目发起人明确同意：
- `git commit`（提交代码）
- `git push`（推送到远程仓库）
- `git pull`（拉取远程代码）
- `git merge`（合并分支）
- `git tag`（打标签）
- `git reset`（回退版本）
- `git branch`（创建/删除分支）
- 任何修改 `.gitignore` 的操作

**开发侧可自主执行的操作**（无需授权）：
- `git status`（查看状态）
- `git log`（查看历史）
- `git diff`（查看差异）
- `git show`（查看内容）

### 2.7 文档同步更新规则

**每次完成功能开发、Bug 修复、版本发布后，必须及时更新 claude.md 文档。**

需要更新的内容：
- 项目当前状态（已完成/待开发功能）
- 版本历史记录
- 变更记录
- 技术决策变更
- 新增的全局规则

**禁止只改代码不更新文档的行为。**

---

## 三、产品定位

### 3.1 一句话描述

浏览器端精简版 Discord，专注**频道实时语音** + **频道图文文字交流**，剔除广告、社交好友、商城、直播等繁杂无关功能。

### 3.2 目标平台

- 电脑端浏览器（Windows / macOS / Linux）
- 手机端浏览器（iOS Safari / Android Chrome 等）

### 3.3 核心差异点

| 功能 | Discord | 本产品 |
|------|---------|--------|
| 好友系统 | 有 | 无 |
| 私信 | 有 | 无 |
| 商城 / Nitro | 有 | 无 |
| 直播 / 活动 | 有 | 无 |
| 机器人 | 有 | 无 |
| 频道创建 | 用户可自由创建 | 需管理员审核/创建 |
| 使用方式 | 需安装客户端或打开网页 | 纯浏览器访问 |
| 视觉风格 | 深色游戏风 | 粉色+绿色 iOS26 液态玻璃拟态 |

---

## 四、核心功能需求

### 4.1 账号体系

#### 4.1.1 初始页面

- 网站打开后首先进入**登录界面**。
- 登录界面提供：账号输入框、密码输入框、登录按钮、注册入口。

#### 4.1.2 注册功能

- 注册表单字段：
  - 用户名（用于登录，唯一，不可重复）
  - 密码
  - 确认密码
  - 对外展示昵称（显示在频道中）
  - 用户头像（上传图片）
- 前端基础校验：密码与确认密码一致、必填项完整。
- 后端校验：用户名唯一性、头像格式与大小限制。
- 注册成功后自动跳转登录页或自动登录（待阶段1确认）。

#### 4.1.3 用户数据管控

- 所有注册用户数据由**后台管理员统一管控**。
- 普通用户无自我管理账号权限（除个人设置中的昵称、头像、音频设备外）。

---

### 4.2 管理员后台系统（基础刚需功能）

管理员账号与普通用户账号**使用同一个登录页面登录**。

#### 4.2.1 登录后的权限选择

- 普通用户登录成功后，直接进入频道主页面。
- 管理员账号登录成功后，系统弹出一个选择窗口，提供两个选项：
  - **进入频道页面**：和普通用户一样进入频道主界面。
  - **进入管理后台**：进入管理员后台界面。
- 后端根据账号角色（`admin` / `user`）返回权限信息，前端据此判断是否展示选择窗口。
- **不再设置独立的管理员登录页面**。

#### 4.2.2 阶段1必须实现

- 统一登录页面（普通用户与管理员共用）。
- 登录接口返回用户角色信息。
- 管理员登录后显示权限选择弹窗。
- 查看全站所有注册用户列表（分页展示）。
- 手动新增用户：填写用户名、密码、昵称、头像，创建普通用户。
- 删除用户：从系统中移除用户账号。
- 管理用户信息：修改昵称、头像、密码重置等。

#### 4.2.3 后续扩展（阶段5之后再追加）

以下功能明确**不在基础版本开发范围内**，待阶段5完成并确认后再规划：

- 频道审核队列
- 聊天记录审计
- 系统日志
- 数据统计面板
- 角色权限细分

---

### 4.3 频道架构（核心结构）

#### 4.3.1 频道创建规则

- **频道为最高交流单元**。
- **新频道创建必须获得管理员许可**，普通用户无法自由创建频道。
- 管理员可在后台创建频道。
- 管理员创建频道时支持两种方式生成频道 ID：
  - 自定义频道 ID（如 `abc123`）
  - 系统自动生成唯一 ID

#### 4.3.2 子频道

- 每个频道内可创建若干：
  - 【语音子频道】
  - 【文字子频道】
- 子频道支持创建完成后随时：
  - 新增
  - 删除
  - 修改名称 / 排序

#### 4.3.3 加入频道

- 用户通过输入**频道 ID** 申请加入对应频道。
- 加入后可在该频道内看到所有子频道列表。

---

### 4.4 语音子频道功能

#### 4.4.1 实时语音通话

- 用户加入语音子频道后，浏览器之间建立实时语音通话。
- 使用 WebRTC 技术实现（具体方案待阶段3技术选型时提供多方案）。

#### 4.4.2 界面展示

- 频道在线人数。
- 所有成员头像。
- 实时说话状态指示器：正在说话的成员头像高亮反馈。

#### 4.4.3 音频控制

- 输入音量调节（麦克风音量）。
- 输出音量调节（扬声器音量）。
- 麦克风增益调节。
- 扬声器增益调节。

#### 4.4.4 两种静音逻辑（必须区分）

| 类型 | 名称 | 效果 | 影响范围 |
|------|------|------|----------|
| 本地静音 | 本地静音 | 自己听不到其他人声音 | 仅影响自己 |
| 闭麦 | 闭麦 | 停止向频道发送自身麦克风音频 | 频道内其他成员听不到自己 |

#### 4.4.5 退出语音

- 点击语音窗口右上角退出按钮离开语音房间。
- 退出后停止音频流，释放麦克风权限。

---

### 4.5 文字子频道功能

#### 4.5.1 消息发送

- 支持发送文字。
- 支持粘贴图片。
- 支持上传图片。
- 支持在语音子频道底部输入框直接发送图文消息，消息同步至对应文字子频道。

#### 4.5.2 聊天记录

- 保存频道聊天历史。
- 聊天记录留存时长由管理员在后台针对**单个频道独立配置**。
- 超出留存时长的消息自动清理或归档（具体策略阶段2确认）。

#### 4.5.3 多窗口叠加显示

- 语音子频道窗口、文字子频道窗口支持**同时打开、叠加悬浮显示**。
- 点击任意窗口自动置顶（z-index 提升）。
- 窗口可拖拽、可关闭、可最小化（具体交互阶段4细化）。

---

### 4.6 用户设置面板

点击顶部导航栏的**设置按钮**弹出设置窗口，包含：

- 音频设备选择：
  - 麦克风设备切换
  - 扬声器设备切换
- 麦克风音量 / 增益调节
- 扬声器音量 / 增益调节
- 修改个人昵称
- 更换头像

---

## 五、UI / 交互规范

### 5.1 主色调

- 粉色（主色）
- 绿色（辅色）
- 整体呈现柔和、卡通、活泼的视觉效果

### 5.2 视觉风格

- **iOS26 液态玻璃拟态效果**（Liquid Glass / Glassmorphism）
- 半透明背景
- 毛玻璃模糊
- 圆润大圆角
- 细腻高光与阴影

### 5.3 动画参考

- iOS 原生系统动画：
  - 弹性过渡
  - 平滑的进入 / 退出
  - 手势驱动的连续动画
- 猛兽派对（Party Animals）柔和卡通动效：
  - 柔软挤压
  - 轻微晃动
  - 可爱、亲和的反馈

### 5.4 顶部导航栏

页面顶部常驻显示：

- 当前所在频道名称
- 频道 ID（支持一键复制）
- 添加频道按钮
- 设置按钮

### 5.5 窗口容器区域

- 导航栏下方为窗口容器区域。
- 子频道、设置均以**悬浮窗口**形式在此区域打开。
- 打开 / 关闭窗口需要平滑过渡动画。
- 支持多窗口叠加，点击任意窗口自动置顶。

---

## 六、阶段1技术栈方案（待项目发起人选择）

> 以下方案仅针对**阶段1**（基础框架 + 登录注册 + 管理员后台 + 基础 UI）。  
> 阶段2~阶段5的技术方案（如实时通信、WebRTC、部署）将在对应阶段单独提供多方案对比。

---

### 方案一：Node.js + Express + React + SQLite（推荐入门）

#### 技术组成

- 前端：React 18 + Vite + Tailwind CSS
- 后端：Node.js + Express
- 数据库：SQLite（单文件数据库，无需单独安装服务）
- 状态管理：React Context / Zustand（轻量）
- 前端路由：React Router
- 后端会话：JWT 或 Session Cookie（阶段1确认）
- 包管理：npm 或 pnpm

#### 优势

- **前后端都是 JavaScript/TypeScript**，学习成本相对低，资料最多。
- **Express 简单直接**，容易理解请求-响应流程。
- **SQLite 无需安装数据库服务**，一个文件即可运行，适合初期开发。
- **Vite 启动快**，热更新体验好。
- 社区庞大，遇到问题容易搜索到解决方案。

#### 劣势

- 前后端分离，项目目录比全栈框架复杂一些。
- 阶段3需要额外引入 WebRTC 和信令服务器，Express 需要扩展。
- 如果未来用户量很大，SQLite 性能可能不足，需要迁移到 PostgreSQL/MySQL。

#### 部署难度

**入门**。

#### 长期运行成本

**低**。初期可在单台服务器上运行，SQLite 免费，Node.js 资源占用小。

#### 阶段1详细部署步骤

1. 安装 Node.js LTS 版本（官网下载安装包）。
2. 创建项目根目录，分为 `frontend/` 和 `backend/` 两个文件夹。
3. 后端：
   - `npm init -y` 初始化项目。
   - 安装 `express`、`cors`、`sqlite3`、`bcryptjs`、`jsonwebtoken`。
   - 创建 `server.js`，配置基础路由。
   - 创建 SQLite 数据库文件 `data.db`，初始化用户表、管理员表。
   - 实现注册、登录、管理员登录、用户 CRUD 接口。
4. 前端：
   - `npm create vite@latest frontend -- --template react`。
   - 安装 `tailwindcss`、`react-router-dom`、`axios`。
   - 配置页面：登录页、注册页、管理员后台页。
   - 对接后端 API。
5. 本地启动：分别运行前端和后端，浏览器访问 `http://localhost:5173`。
6. 生产部署：将前端构建为静态文件，由后端或 Nginx 提供访问；后端使用 `pm2` 长期运行。

---

### 方案二：Next.js 全栈 + Prisma + SQLite（一体化框架）

#### 技术组成

- 全栈框架：Next.js 14+（App Router）
- 数据库 ORM：Prisma
- 数据库：SQLite（开发）/ PostgreSQL（生产可迁移）
- 样式：Tailwind CSS
- 认证：NextAuth.js 或自定义 JWT

#### 优势

- **一个项目同时包含前端和后端**，目录结构紧凑。
- Next.js 自动处理路由、API、构建、部署。
- Prisma 提供清晰的数据库模型定义，自动迁移。
- 部署到 Vercel 等平台非常方便（但实时语音阶段需要额外服务器）。

#### 劣势

- Next.js 概念较多（App Router、Server Components、Server Actions），零基础理解门槛略高。
- 阶段3的 WebRTC 信令服务器不能部署在纯 Serverless 平台，需要单独服务器。
- 全栈框架一旦出错，排查范围同时涉及前后端。

#### 部署难度

**中等**。

#### 长期运行成本

**中**。开发阶段成本低；阶段3后需要一台持续运行的服务器做 WebRTC 信令，成本上升。

#### 阶段1详细部署步骤

1. 安装 Node.js LTS 版本。
2. 执行 `npx create-next-app@latest 项目名`。
3. 安装 Prisma：`npm install prisma @prisma/client`。
4. 初始化 Prisma：`npx prisma init`，配置 SQLite 数据源。
5. 在 `prisma/schema.prisma` 中定义 User、Admin 模型。
6. 执行 `npx prisma migrate dev` 生成数据库。
7. 在 `app/api/` 下创建注册、登录、管理员登录、用户 CRUD 的 API 路由。
8. 在 `app/` 下创建登录页、注册页、管理员后台页面。
9. 本地运行 `npm run dev`，访问 `http://localhost:3000`。
10. 生产构建：`npm run build` + `npm start`，配合 PM2 或 Docker 部署。

---

### 方案三：Python FastAPI + Vue3 + SQLite（后端 Python，前端 Vue）

#### 技术组成

- 前端：Vue 3 + Vite + Tailwind CSS
- 后端：Python 3.11+ + FastAPI
- 数据库：SQLite + SQLAlchemy
- 前端路由：Vue Router
- 状态管理：Pinia
- 后端异步：FastAPI 原生支持 async/await

#### 优势

- **Python 语法简洁**，后端代码对零基础更友好。
- FastAPI 自动生成接口文档（`/docs`），方便调试。
- Vue 3 的模板语法接近 HTML，容易上手。
- FastAPI 性能优秀，适合后续 WebSocket 实时通信。

#### 劣势

- 需要同时学习 Python 和 JavaScript/TypeScript 两套语言。
- Python 部署相对 Node.js 多一步环境配置（虚拟环境、依赖安装）。
- 前端生态资料比 React 略少。

#### 部署难度

**中等**。

#### 长期运行成本

**低 ~ 中**。Python 后端资源占用略高于 Node.js，但单台服务器仍可运行。

#### 阶段1详细部署步骤

1. 安装 Python 3.11+ 和 Node.js LTS。
2. 创建项目根目录，分为 `frontend/` 和 `backend/`。
3. 后端：
   - 创建 Python 虚拟环境：`python -m venv venv`。
   - 激活虚拟环境，安装 `fastapi`、`uvicorn`、`sqlalchemy`、`passlib`、`python-jose`、`python-multipart`。
   - 创建 `main.py`，配置 FastAPI 应用。
   - 使用 SQLAlchemy 定义 User、Admin 模型，创建 SQLite 数据库。
   - 实现注册、登录、管理员登录、用户 CRUD 接口。
4. 前端：
   - `npm create vue@latest frontend`。
   - 安装 Tailwind CSS、Vue Router、Pinia、axios。
   - 创建登录页、注册页、管理员后台页。
   - 对接 FastAPI 接口。
5. 本地启动：分别运行后端 `uvicorn main:app --reload` 和前端 `npm run dev`。
6. 生产部署：后端使用 `gunicorn` + `uvicorn`  workers；前端构建静态文件由 Nginx 或后端提供。

---

### 方案四：纯前端无后端方案（仅阶段1临时演示，不推荐长期使用）

#### 技术组成

- 前端：React 或 Vue + Vite
- 数据存储：浏览器 LocalStorage / IndexedDB
- 无真实后端、无数据库、无管理员权限校验

#### 优势

- 部署最简单，直接打开 HTML 文件即可运行。
- 无需服务器，零成本。

#### 劣势

- **不安全**：所有数据存在用户浏览器中，无法真正管理用户。
- **无法多人使用**：每个浏览器数据独立。
- **无法满足后续阶段**：阶段2之后必须重写为真实后端。
- 严格来说不能称为完整产品。

#### 部署难度

**极简**。

#### 长期运行成本

**零**。

#### 适用说明

本方案**仅可用于阶段1的 UI 原型演示**，不能作为最终产品方案。如果发起人希望先看到界面效果，可以临时采用，但必须在阶段2之前迁移到真实后端方案（方案一/二/三）。

---

## 七、项目目录结构规划

> 以下结构为**通用规划**，最终结构会根据选定的技术栈微调。  
> 核心原则是：**前后端分离、配置独立、职责清晰、中文注释充分**。

```
项目根目录/
├── README.md                    # 项目说明
├── claude.md                    # 本产品需求文档（PRD）
├── .env.example                 # 环境变量示例文件（敏感信息模板）
├── .gitignore                   # Git 忽略规则
├── docs/                        # 文档目录
│   ├── deploy.md                # 部署说明
│   ├── api.md                   # 接口文档
│   └── ui-guide.md              # UI/UX 规范补充
│
├── backend/                     # 后端代码目录
│   ├── src/
│   │   ├── config/              # 配置读取（数据库、端口、密钥等）
│   │   ├── controllers/         # 控制器：处理请求参数与响应
│   │   ├── models/              # 数据模型定义
│   │   ├── routes/              # 路由注册
│   │   ├── services/            # 业务逻辑层
│   │   ├── middlewares/         # 中间件（认证、日志、错误处理等）
│   │   ├── utils/               # 工具函数
│   │   └── app.js / main.py     # 应用入口
│   ├── tests/                   # 后端测试
│   ├── migrations/              # 数据库迁移文件
│   └── package.json / requirements.txt / pyproject.toml
│
├── frontend/                    # 前端代码目录
│   ├── public/                  # 静态资源
│   ├── src/
│   │   ├── api/                 # 后端接口封装
│   │   ├── assets/              # 图片、字体、图标
│   │   ├── components/          # 公共组件
│   │   │   ├── ui/              # 基础 UI 组件（按钮、输入框、弹窗）
│   │   │   ├── layout/          # 布局组件（导航栏、窗口容器）
│   │   │   └── channel/         # 频道相关组件
│   │   ├── pages/               # 页面级组件
│   │   │   ├── LoginPage/
│   │   │   ├── RegisterPage/
│   │   │   ├── AdminPage/
│   │   │   ├── ChannelPage/
│   │   │   └── SettingsPage/
│   │   ├── stores/              # 全局状态管理
│   │   ├── hooks/               # 自定义 Hooks / Composables
│   │   ├── utils/               # 工具函数
│   │   ├── styles/              # 全局样式、主题变量
│   │   └── main.js / main.tsx   # 前端入口
│   └── package.json
│
└── docker-compose.yml（可选）    # 一键部署配置
```

---

## 八、部署与运行通用要求

### 8.1 开发环境要求

- 电脑一台（Windows / macOS / Linux 均可）
- 网络连接
- 根据所选方案安装 Node.js 或 Python
- 现代浏览器（Chrome / Edge / Firefox / Safari）

### 8.2 麦克风权限说明

浏览器对麦克风访问有严格安全限制：

- **localhost**：允许调用麦克风，无需 https。
- **非 localhost**：必须使用 **https://**，否则浏览器会拒绝麦克风权限。
- 因此，任何外网部署或手机测试，都必须配置 SSL 证书（https）。

### 8.3 生产部署最小清单

- 一台云服务器（如阿里云、腾讯云、AWS、Vercel 等）。
- 一个域名（可选但推荐，便于配置 https）。
- SSL 证书（可使用 Let's Encrypt 免费证书）。
- 进程守护工具（Node.js 用 PM2，Python 用 systemd 或 supervisor）。
- 反向代理（Nginx 或 Caddy）。

---

## 九、后续阶段概要

### 阶段2重点

- 频道数据模型设计
- 管理员创建/管理频道与文字子频道
- 用户通过频道 ID 加入频道
- 文字消息发送、图片上传/粘贴
- 消息持久化与按频道独立留存策略

### 阶段3重点

- WebRTC 实时语音方案选型（至少2套方案）
- 语音子频道创建与加入
- 音频输入/输出控制
- 本地静音与闭麦逻辑
- 说话状态检测与高亮反馈

### 阶段4重点

- 悬浮多窗口系统
- 窗口层级管理（点击置顶）
- iOS26 液态玻璃拟态视觉实现
- 动画系统优化
- 个人设置面板完善

### 阶段5重点

- 全站 BUG 修复
- 移动端浏览器适配
- 性能优化
- 安全加固
- 管理员后台扩展评估

---

## 十、项目当前状态

### 已确认事项

1. **项目名称**：喵喵语音 🐾
2. **技术栈方案**：方案三（Python FastAPI + Vue3 + SQLite）
3. **开发语言**：JavaScript（前端）+ Python（后端）
4. **认证方式**：JWT Token
5. **数据库**：SQLite（当前）/ 后续可迁移
6. **GitHub 仓库**：https://github.com/WumiMaster/MMV

### 已完成功能

| 阶段 | 功能 | 状态 |
|------|------|------|
| 阶段1 | 用户注册/登录 | ✅ |
| 阶段1 | 管理员后台（用户/频道管理） | ✅ |
| 阶段1 | 基础 UI（iOS26 液态玻璃风格） | ✅ |
| 阶段2 | 频道创建与管理 | ✅ |
| 阶段2 | 文字子频道聊天 | ✅ |
| 阶段2 | 图片上传/粘贴 | ✅ |
| 阶段2 | 消息持久化与留存策略 | ✅ |
| 阶段3 | WebRTC P2P 语音通话 | ✅ |
| 阶段3 | 语音提示音 | ✅ |
| 阶段3 | 说话状态检测 | ✅ |
| 阶段3 | 网络速度监控 | ✅ |
| 优化 | 图片预览缩放/拖拽 | ✅ |
| 优化 | 新消息提示（滚动到底部） | ✅ |
| 优化 | 频道/用户头像管理 | ✅ |
| 优化 | 频道 ID 编辑 | ✅ |
| 优化 | 麦克风电平指示器 | ✅ |

### 待开发功能

| 阶段 | 功能 |
|------|------|
| 阶段4 | 悬浮多窗口系统 |
| 阶段4 | 窗口层级管理 |
| 阶段4 | 动画系统优化 |
| 阶段5 | 移动端浏览器适配 |
| 阶段5 | 性能优化与安全加固 |

### 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-07-28 | 初始版本，完成阶段1-3基础功能 |
| v1.01 | 2026-07-28 | WebSocket 断线重连、Git 操作规则 |
| v1.02 | 2026-07-28 | 语音功能优化、网络监控、提示音 |
| v1.03 | 2026-07-28 | 更新项目状态文档（v1.02 补充版本） |
| v1.1 | 2026-07-29 | 大版本更新：图片预览优化、新消息提示、头像管理、频道ID编辑、麦克风电平指示器 |
| v1.11 | 2026-07-29 | 阶段4：悬浮窗口系统、频道切换自动打开子频道、UI细节优化 |
| v1.2 | 2026-07-29 | 响应式布局、iOS风格弹窗动画、滚动条美化、提示音优化、网络监控增强 |

---

## 十一、变更记录

| 版本 | 日期 | 变更内容 | 负责人 |
|------|------|----------|--------|
| v1.0 | 2026-07-27 | 初始版本，整理全部需求 | Claude Code |
| v1.01 | 2026-07-28 | 添加 Git 操作授权规则 | Claude Code |
| v1.02 | 2026-07-28 | 更新项目状态与版本历史 | Claude Code |
| v1.03 | 2026-07-28 | 更新项目状态文档 | Claude Code |
| v1.1 | 2026-07-29 | 大版本更新，多项功能优化 | Claude Code |
| v1.11 | 2026-07-29 | 阶段4开发，悬浮窗口系统 | Claude Code |
| v1.2 | 2026-07-29 | 响应式布局、UI动画优化 | Claude Code |
| v1.1 | 2026-07-29 | 大版本更新，多项功能优化 | Claude Code |
| v1.11 | 2026-07-29 | 阶段4开发，悬浮窗口系统，UI优化 | Claude Code |

---

> **本文档为项目最高依据，任何开发行为前请先核对本文档。**
